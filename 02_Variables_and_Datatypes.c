#include <stdio.h>

// datatype variableName = value;
// datatype variableName;

int main()
{
    // int integer1, integer2, sum;
    // printf("Enter first number: ");
    // scanf("%d", &integer1);
    // printf("Enter second number: ");
    // scanf("%d", &integer2);
    // sum = integer1 + integer2;
    // printf("The sum of %d and %d is: %d", integer1, integer2, sum);

    char name;
    printf("Enter a digit: ");
    scanf("%c", &name);
    printf("Your name's letter is: %c", name);

    // double integer1, integer2, sum;
    // printf("Enter first number: ");
    // scanf("%d", &integer1);
    // printf("Enter second number: ");
    // scanf("%d", &integer2);
    // sum = integer1 + integer2;
    // printf("The sum of %d and %d is: %d", integer1, integer2, sum);

    return 0;
}
